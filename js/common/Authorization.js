/** Authorization(로그인 관리) 클래스 정의 */
class Authorization {
    /** 로그인 */
    login(id, pass) {
        /** 1. 로그인 시도 */
        /** 2. session, body 저장 */
    }
    /** 로그인 후 */
    loginAfter(roleData) {
        /** 1. 로그인 이후 권한 설정 */
    }
    /** 로그 아웃 */
    logOut(id) {
        /** 1. 로그아웃 시도 */
        /** 2. session, body 삭제 */
    }
}
export default new Authorization();
